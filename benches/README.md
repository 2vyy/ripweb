# Bench Layout

The `benches/` directory should contain benchmark code only.

Shared benchmark input data lives in `../corpus/`.

This keeps performance harnesses separate from reusable fixture data.
